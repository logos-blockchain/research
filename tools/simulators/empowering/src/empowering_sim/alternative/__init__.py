"""ALTERNATIVE designs. **Not part of the base model, and not part of any base result.**

Everything in this package explores a change to the specified mechanism. Nothing here is
imported by the base modules, nothing here feeds the working-region maps unless a caller
asks for it explicitly, and every figure produced here must be labelled as a variant
wherever it is quoted.

The separation is physical rather than conventional because the tokenomics report keeps
candidate changes above the line for the same reason: a variant result that leaks into the
base is worse than no variant at all.
"""
